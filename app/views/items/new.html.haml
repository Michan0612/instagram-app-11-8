.item_inner
  - @item.errors.full_messages.each do |message|
    %p.error_message= message
  = form_with model: @item, url: items_path, method: :post, local: true do |f|
    .form_header
      = link_to 'Cancel', root_path, class: 'post_cancel'
      %h1.header_name Status
      .actions
        = f.submit :Post, class: 'post_btn'
    .user_ui
      = image_tag current_user&.avatar_image, class: 'avatar_present_img'
      %p.user_name= current_user&.username
    .item_content_field
      %label.content
      = f.text_field :content, class: 'post_content', placeholder: 'What\'s on your mind?'
    .post_image_field
      = f.label :photos, '+ Album', class: "image_input_btn"
      = f.file_field :photos, multiple: true, class: 'post_photos'